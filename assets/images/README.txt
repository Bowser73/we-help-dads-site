IMAGES FOLDER
=============

Drop photography and image assets here. Suggested files to provide:

- og-image.jpg        (1200x630)  Social share preview image. Referenced in index.html <head>.
- hero-fallback.jpg   Optional still image fallback for the hero on very low-power devices.
- board photos        Headshots for the Board of Directors cards once names are confirmed.

Until real images are supplied, the site uses CSS/canvas-generated visuals and SVG
placeholders, so it looks complete without any photography.

Replace/extend as needed and update the references in index.html.
